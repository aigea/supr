import { MathsyValidatorDirective } from './mathsy-validator.directive';

describe('MathsyValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MathsyValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
